<%= form_with(method: :get, url:tests_path, id: "wav-file") do |f| %>
<%= f.collection_select(:id, Test.all, :id, :id, {:prompt => "Please Select a Sector"}, {:multiple => true}) %>
<%= f.submit "選択" %>
<% end %>